apiVersion: v1
kind: ConfigMap
metadata:
  name: flannel-config
  namespace: kube-system
  labels:
    tier: node
    k8s-app: flannel
data:
  cni-conf.json: |
    {
      "name": "cbr0",
      "cniVersion": "0.3.1",
      "plugins": [
        {
          "type": "flannel",
          "delegate": {
            "hairpinMode": true,
            "isDefaultGateway": true
          }
        },
        {
          "type": "portmap",
          "capabilities": {
            "portMappings": true
          }
        }
      ]
    }
  net-conf.json: |
    {
      "EnableIPv4": ${enable_ipv4},
      "Network": "${pod_cidr_v4}",
      "EnableIPv6": ${enable_ipv6},
      "IPv6Network": "${pod_cidr_v6}",
      "Backend": {
        "Type": "vxlan",
        "Port": 8472
      }
    }
